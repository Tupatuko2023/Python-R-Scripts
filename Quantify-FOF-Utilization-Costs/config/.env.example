# Local environment configuration (DO NOT COMMIT secrets or real paths)
# Set DATA_ROOT to a repo-external secure directory containing the input data.
DATA_ROOT=__SET_ME__

# Aggregates are disabled by default. Enable only with explicit permission.
ALLOW_AGGREGATES=0

# Output directory root (repo-relative recommended)
OUTPUT_DIR=outputs
