# Copy to config/.env (DO NOT COMMIT)
# DATA_ROOT points to a repo-external directory containing sensitive inputs.
DATA_ROOT=/absolute/or/termux/path/to/secure_data_root

# Optional: logical subfolders (relative to DATA_ROOT)
PAPER_02_DIR=paper_02

# Safety gate: aggregates are OFF by default.
ALLOW_AGGREGATES=0
